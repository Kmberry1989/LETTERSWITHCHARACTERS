{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's public profile information.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "photoURL": {
          "type": "string",
          "description": "The URL of the user's profile picture."
        },
        "email": {
          "type": "string",
          "description": "The user's email address."
        },
        "totalScore": {
          "type": "number",
          "description": "The user's total leaderboard score."
        },
        "avatarId": {
          "type": "string",
          "description": "The ID of the user's selected avatar image."
        },
        "tileSetId": {
          "type": "string",
          "description": "The ID of the user's selected tile set."
        },
        "boardThemeId": {
          "type": "string",
          "description": "The ID of the user's selected board theme."
        }
      },
      "required": ["displayName", "email"]
    },
    "Tile": {
      "title": "Tile",
      "description": "Represents a single game tile.",
      "type": "object",
      "properties": {
        "letter": { "type": "string" },
        "score": { "type": "number" },
        "isBlank": { "type": "boolean", "description": "Whether the tile was originally a blank." }
      },
      "required": ["letter", "score"]
    },
     "ChatMessage": {
      "title": "Chat Message",
      "description": "Represents a single chat message in a game.",
      "type": "object",
      "properties": {
        "senderId": { "type": "string" },
        "senderName": { "type": "string" },
        "text": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["senderId", "senderName", "text"]
    },
    "Game": {
      "title": "Game",
      "description": "Represents a single game session.",
      "type": "object",
      "properties": {
        "players": {
          "type": "array",
          "items": { "type": "string" },
          "description": "An array of user UIDs participating in the game."
        },
        "playerData": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "displayName": { "type": "string" },
              "score": { "type": "number" },
              "avatarId": { "type": "string" },
              "tiles": {
                "type": "array",
                "items": { "$ref": "#/entities/Tile" }
              },
              "hintUsed": { "type": "boolean" }
            }
          }
        },
        "board": {
            "type": "object",
            "description": "The state of the game board. Keys are 'row-col', values are Tile objects.",
            "additionalProperties": {
                "$ref": "#/entities/Tile"
            }
        },
        "tileBag": {
            "type": "array",
            "items": { "$ref": "#/entities/Tile" },
            "description": "The remaining tiles in the game."
        },
        "currentTurn": {
          "type": "string",
          "description": "The UID of the player whose turn it is."
        },
        "status": {
          "type": "string",
          "enum": ["active", "pending", "finished"],
          "description": "The current status of the game."
        },
        "consecutivePasses": {
          "type": "number",
          "description": "The number of consecutive passes made by players."
        },
        "winner": {
            "type": "string",
            "description": "The UID of the winning player."
        },
        "messages": {
          "type": "array",
          "items": { "$ref": "#/entities/ChatMessage" },
          "description": "A list of chat messages for the game."
        }
      },
      "required": ["players", "playerData", "board", "tileBag", "currentTurn", "status"]
    }
  },
  "auth": {
    "providers": ["google.com", "password", "anonymous"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores public profile information for each user."
    },
    "/games/{gameId}": {
      "schema": {
        "$ref": "#/entities/Game"
      },
      "description": "Stores all the data for a single game session."
    }
  }
}
